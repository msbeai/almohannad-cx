# 🖊️ دليل تحرير almohannad.cx بنفسك

> **الخلاصة في سطرين:**
> ✅ **للمقالات والكتب والبودكاست:** افتح [app.pagescms.org](https://app.pagescms.org) → عدّل → Save → يُنشر تلقائيًا خلال دقيقتين.
> ✅ **لأي شيء آخر** (الصفحة الرئيسية، عنّي، الألوان، التذييل): عدّل الملف على جهازك ثم `git push` — أو اطلبه من Claude.

---

## الطريقة الأولى: التحرير من المتصفح (الأسهل — بلا Terminal)

موقعك مربوط بمحرّر **Pages CMS**: نماذج جاهزة في المتصفح، تحفظ فيُنشر الموقع تلقائيًا.

### خطوات الدخول
1. افتح **[app.pagescms.org](https://app.pagescms.org)**
2. سجّل الدخول بحساب **GitHub** (حسابك: `msbeai`)
3. اختر مستودع **`almohannad-cx`**

### ماذا تستطيع أن تحرّر هناك؟

| القسم في المحرّر | ماذا يغطي |
|---|---|
| **المقالات (عربي)** | كل مقالات الموقع العربية — عنوان، تصنيف، تاريخ نشر، والنص بمحرّر غني |
| **Articles (English)** | الترجمات الإنجليزية (أنشئ ملفًا بنفس الاسم تمامًا ليرتبطا) |
| **الكتب** | قائمة الكتب: العنوان، السنة، الغلاف، النبذة، **وملف الـ PDF للتحميل** |
| **البودكاست** | الحلقات: العنوان، البودكاست، رابط يوتيوب، النبذة |
| **مدونة (نسخة احتياطية)** | أرشيف منشورات مدونة ثمانية |

### كيف يُنشر التعديل؟
تضغط **Save** → يُحفظ في GitHub → **GitHub Action** يبني الموقع وينشره على نتلفاي.
**انتظر دقيقة إلى دقيقتين** ثم حدّث almohannad.cx وسترى تعديلك حيًّا. لا خطوة إضافية.

---

## الطريقة الثانية: تحرير الملفات على جهازك

المشروع في: `~/Projects/almohannad-cx`
افتح أي ملف بـ **VS Code** (أو TextEdit)، عدّل، احفظ، ثم انشر.

### أين أعدّل ماذا؟

| ماذا تريد؟ | أين؟ |
|---|---|
| مقال عربي | `src/content/ar/اسم-المقال.md` |
| الترجمة الإنجليزية | `src/content/en/نفس-الاسم.md` |
| الكتب | `src/data/books.json` (+ ضع ملف الـ PDF في `public/books/`) |
| حلقات البودكاست | `src/data/podcasts.json` |
| نص الصفحة الرئيسية + شريط الأرقام | `src/pages/index.astro` (الإنجليزية: `src/pages/en/index.astro`) |
| بطاقات «عنّي» المتأرجحة | `src/pages/about.astro` — **عدّل النص داخل البطاقات فقط**، فالبطاقات تفاعلية وبمقاس ثابت: أبقِ النص قصيرًا |
| التذييل (البريد، الترخيص، الروابط) | النصوص في `src/site.ts` والبنية في `src/layouts/Base.astro` |
| عناوين القوائم والتصنيفات | `src/site.ts` |
| ألوان الموقع كاملة | أول ٩٠ سطرًا من `src/styles/global.css` (متغيرات `:root` والوضع الداكن) |

### النشر بعد التعديل — أمر واحد

```bash
cd ~/Projects/almohannad-cx && git add -A && git commit -m "تحديث المحتوى" && git push
```

GitHub Action يتولى البناء والنشر تلقائيًا خلال دقيقتين.
> ⚠️ لا تستخدم `publish.sh` القديم — النشر الآن عبر `git push` فقط.

### معاينة قبل النشر (اختياري)

```bash
cd ~/Projects/almohannad-cx && npm run dev
```

ثم افتح `http://localhost:4321` — كل حفظ يظهر فورًا. أوقفها بـ `Ctrl+C`.

---

## إضافة مقال جديد — القالب

أنشئ ملفًا في `src/content/ar/` باسم إنجليزي قصير بشرطات (يصبح رابط المقال)، مثل `my-new-article.md`:

```markdown
---
title: "عنوان المقال بالعربية"
titleAr: "عنوان المقال بالعربية"
titleEn: "English Title"
theme: cx
order: 99
linkedin: ""
date: 2026-06-29
---

## تمهيد

نص المقال هنا. سطر فارغ بين كل فقرة وأخرى.
```

- `theme:` إما `cx` (تجربة العميل) أو `research` (الأبحاث التسويقية)
- `date:` تاريخ النشر — يظهر على البطاقة ويُستخدم في الترتيب من الأحدث للأقدم
- الصور: ضعها في `public/images/articles/` وأدرجها هكذا مع **وصف للصورة**: `![وصف الصورة](/images/articles/الاسم.jpg)`
- **الغلاف يتولد تلقائيًا** عند النشر
- للترجمة: ملف **بنفس الاسم تمامًا** في `src/content/en/`

## إضافة كتاب

في `src/data/books.json` أضف داخل `"books": [ ]`:

```json
{
  "titleAr": "اسم الكتاب",
  "titleEn": "Book Title",
  "year": "2026",
  "cover": "/images/books/cover.jpg",
  "descriptionAr": "نبذة قصيرة.",
  "file": "/books/book-name.pdf"
}
```

- الغلاف في `public/images/books/` وملف الـ PDF في `public/books/`
- احذف سطر `"file"` إن لم ترد زر تحميل
- انتبه للفاصلة `,` بين كتاب وآخر

## إضافة حلقة بودكاست

في `src/data/podcasts.json` داخل `"episodes": [ ]`:

```json
{
  "titleAr": "عنوان الحلقة",
  "titleEn": "Episode Title",
  "show": "اسم البودكاست",
  "year": "2026",
  "youtube": "https://www.youtube.com/watch?v=XXXXXXXX",
  "descriptionAr": "نبذة قصيرة."
}
```

الفيديو يظهر مضمّنًا تلقائيًا.

---

## ⚠️ قواعد ذهبية

1. **لا ترفع المشروع إلى مستودع عام** — خطوط ثمانية ترخيصها يمنع إعادة التوزيع (المستودع الخاص الحالي آمن).
2. في ملفات `.md`: لا تلمس السطور بين `---` و `---` أول الملف إلا بمعرفة القيم.
3. في ملفات `.json`: فاصلة ناقصة أو زائدة تكسر الصفحة — المعاينة تكشفها قبل النشر.
4. الصفحات المصمَّمة (الرئيسية، عنّي، صفحة المقالات) فيها كود تفاعلي — عدّل **النصوص** بحرية، واترك الوسوم `< >` والأقواس `{ }` كما هي.
5. **لو انكسر شيء:** كل شيء محفوظ في git. اطلب من Claude: «أرجع آخر نسخة سليمة» — أو بنفسك:
   ```bash
   cd ~/Projects/almohannad-cx && git checkout -- . 
   ```
   (يلغي كل تعديل غير منشور)

---

## متى تلجأ إلى Claude؟

- تغيير تصميم أو تخطيط (وليس مجرد نص)
- إضافة صفحة أو قسم جديد
- أي شيء في `Base.astro` أو `global.css` أو الكود التفاعلي
- استرجاع نسخة سابقة أو إصلاح عطل

*آخر تحديث للدليل: يونيو 2026 — يعكس: محرر Pages CMS، النشر عبر GitHub Action، تحميل الكتب، تواريخ المقالات وترتيبها، بطاقات «عنّي» المتأرجحة، والتذييل الجديد.*
